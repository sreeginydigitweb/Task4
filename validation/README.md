# validation

How the work was checked, and what the checks returned.

Validation covers both the mechanical (does the route return valid HTML, is
every value escaped, do the tests pass) and the factual (does the page show
real `ledsone` data, and does it avoid claiming anything the database does not
say).

Contains: `product-keywords-validation.md`.

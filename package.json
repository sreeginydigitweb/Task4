{
  "name": "task4-product-keywords",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only HTML view of product and keyword data from the existing ledsone database.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node product-keywords/server.js",
    "test": "node --test --test-concurrency=1 \"product-keywords/**/*.test.js\""
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}

# sql

The SQL this application actually runs, kept readable outside the JavaScript.

Every statement here is a SELECT. There is no INSERT, UPDATE, DELETE, CREATE,
ALTER, DROP or TRUNCATE in this project, and the connection it runs on is
read-only at the server.

Contains: `product-keywords.sql`.

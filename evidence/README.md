# evidence

Proof that the things claimed elsewhere are actually true.

Query output, HTTP responses, test runs and connection checks - captured as
they were produced, with the date and the command that produced them. Evidence
is recorded, not summarised: a reader should be able to run the same command
and compare.

Contains: `build-verification.md`.

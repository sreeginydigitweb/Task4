# duplicate-risk-reports

What already existed before this project was built, and what was reused rather
than rewritten.

The purpose is to stop the same thing being built twice across the estate.
Each report names the existing file, what it does, and whether this project
reused it, extended it, or deliberately built its own.

Contains: `task-4-duplicate-risk.md`.

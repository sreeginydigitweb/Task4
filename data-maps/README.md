# data-maps

Column-by-column maps from what a screen shows to where the value came from.

Each map names the database, the schema, the table and the column for every
field on a page, and marks anything that is derived, joined or unavailable.
A field with no source is recorded as having no source, never as a guess.

Contains: `product-keywords-data-map.md`.

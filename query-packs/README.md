# query-packs

Grouped, reusable read-only queries with a note on what each one answers.

A query pack is for investigation - checking coverage, confirming a
relationship, sizing a table - rather than for the application itself. The
application's own SQL lives in `sql/`.

Contains: `keyword-source-investigation.md`.

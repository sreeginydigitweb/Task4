# workflows

The order things are done in, and by whom.

For Task 4 the workflow is: discovery, then build, then review, then commit on
explicit confirmation. Nothing is committed or pushed without it.

Contains: `task-4-workflow.md`.
